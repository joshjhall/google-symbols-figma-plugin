# Container Build System Environment Variables
# Copy this file to .env and update with your values

# 1Password Integration Configuration
# Copy this file to .env and fill in your values

# Required: Your 1Password service account token
# Get one at: https://developer.1password.com/docs/service-accounts/
# OP_SERVICE_ACCOUNT_TOKEN=ops_your_token_here

# Optional: Your 1Password account URL (if different from default)
# OP_ACCOUNT=foo.1password.com

# Optional: Custom names for items in 1Password
# If not set, the script will try common default names
# GIT_SIGNING_KEY_ITEM="Git Signing or Auth SSH Key"
# GIT_CONFIG_ITEM="Git Configuration"

# GitHub Tokens
# The plugin uses two separate tokens for security and least-privilege access:
#
# 1. Release Token (for creating releases and uploading assets)
#    Required scopes: repo (read/write)
#    Stored in 1Password as: "GitHub (Figma icon plugin releases)"
#    GITHUB_RELEASE_TOKEN=ghp_your_release_token_here
#
# 2. Version Check Token (for reading public repo data)
#    Required scopes: (public read - no special scopes needed)
#    Stored in 1Password as: "GitHub (Version check)"
#    GITHUB_VERSION_CHECK_TOKEN=ghp_your_version_check_token_here
#
# Token Priority (automatic fallback):
#   1. 1Password CLI (op read "op://Private/<item>/credential")
#   2. Environment variable (GITHUB_RELEASE_TOKEN, GITHUB_VERSION_CHECK_TOKEN)
#   3. .env file (this file)
#
# Recommendation: Store tokens in 1Password for best security
# See docs/github-token-setup.md for detailed setup instructions
